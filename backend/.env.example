# Copy to .env on the server and fill in. Never commit .env.
AIRLABS_API_KEY=
# Optional: turn the Umetrip fallback off entirely
UMETRIP_ENABLED=true
# Optional: shared secret the app must send as X-Airadar-Token
APP_TOKEN=change-me
